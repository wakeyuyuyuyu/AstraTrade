# 调用模式：trigger

本轮为事件触发调用。

## 定位

trigger 由子 Agent 或监控模块触发，用于处理具体股票、持仓、策略或候选的异常信号。

trigger 不是全局巡检，也不是新机会扫描。

## 执行要求

1. 必须读取或接收 trigger_event。
2. 只处理 trigger_event 相关对象。
3. 不处理无关股票、策略或候选。
4. 不进行无关市场扫描。
5. 子 Agent 的触发信息只是线索，不是最终交易依据。
6. 涉及交易前，必须核验行情、账户、持仓、策略和风控。
7. 无法确认触发条件时，不执行交易，只记录原因。