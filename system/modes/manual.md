# 调用模式：manual

本轮为人工调用。

## 定位

manual 用于执行用户给定的自然语言任务。

用户任务是本轮最高优先级。

## 执行要求

1. 优先完成用户任务。
2. 不默认执行完整自动巡检。
3. 不默认读取 phase prompt。
4. 只读取完成任务所需的文件。
5. 用户任务涉及交易、持仓、策略、候选或风控时，必须读取相关状态。
6. 用户任务涉及行情、财务、新闻、公告、研报时，必须使用工具或 skill。
7. 即使用户直接要求买入或卖出，也不能跳过风控和记录。